import {Turtle} from './Turtle';

// This launcher code is most likely obsolete, once the code is invoked from a webui.
// Testing generating a Curve, using a turtle and the generateCurve method.
const turtle: Turtle = new Turtle(0, 0, 0);
console.log(turtle.getHeading());