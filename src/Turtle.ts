class Turtle {
  heading: number;
  x: number;
  y: number;

  constructor(heading: number, x: number, y: number) {
    this.heading = heading;
    this.x = x;
    this.y = y;
  }
}

export { Turtle };

